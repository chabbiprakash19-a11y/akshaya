/* =====================================================
   BASIC RESET
===================================================== */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: "DM Sans", sans-serif;
    background: #f7faf8;
    color: #289441;
    line-height: 1.6;
}

a {
    text-decoration: none;
    color: inherit;
}

button,
input,
textarea,
select {
    font-family: inherit;
}

.container {
    width: min(1180px, 92%);
    margin: auto;
}

.section {
    padding: 100px 0;
}


/* =====================================================
   TOP BAR
===================================================== */

.top-bar {
    background: #21914a;
    color: #cce1dc;
    font-size: 11px;
}

.top-content {
    min-height: 38px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
}

.top-content div {
    display: flex;
    gap: 10px;
}

.top-content a:hover {
    color: white;
}


/* =====================================================
   NAVBAR
===================================================== */

.navbar {
    position: absolute;
    top: 38px;
    left: 0;
    width: 100%;

    z-index: 100;

    padding: 18px 0;

    background: rgba(0, 50, 47, 0.2);

    backdrop-filter: blur(12px);

    border-bottom: 1px solid rgba(255,255,255,0.15);
}

.nav-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
}


/* LOGO */

.logo {
    display: flex;
    align-items: center;
    gap: 10px;

    color: white;
}

.logo-icon {
    width: 42px;
    height: 42px;

    border-radius: 12px;

    display: flex;
    align-items: center;
    justify-content: center;

    background: #d3f0e3;
    color: #33aa65;

    font-size: 26px;
    font-weight: 700;
}

.logo strong {
    display: block;

    font-family: "Playfair Display", serif;

    font-size: 22px;

    line-height: 1;
}

.logo small {
    display: block;

    font-size: 8px;

    letter-spacing: 3px;

    margin-top: 4px;
}


/* NAV LINKS */

.nav-links {
    display: flex;
    gap: 21px;
}

.nav-links a {
    color: white;

    font-size: 11px;

    transition: 0.3s;
}

.nav-links a:hover {
    color: #b8ead5;
}


.nav-button {
    padding: 11px 17px;

    background: white;
    color: #087066;

    border-radius: 30px;

    font-size: 11px;
    font-weight: 700;

    white-space: nowrap;

    transition: 0.3s;
}

.nav-button:hover {
    transform: translateY(-2px);
}


/* =====================================================
   HERO
===================================================== */

.hero {
    min-height: 760px;

    position: relative;

    display: flex;
    align-items: center;

    color: white;

    overflow: hidden;
}

.hero-overlay {
    position: absolute;
    inset: 0;

    z-index: -1;

    background:
        linear-gradient(
            90deg,
            rgba(28, 154, 95, 0.97),
            rgba(8, 94, 78, 0.78),
            rgba(8, 115, 74, 0.25)
        ),
        url("https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?auto=format&fit=crop&w=2000&q=90");

    background-size: cover;
    background-position: center;
}

.hero-text {
    max-width: 690px;

    padding-top: 80px;
}

.badge {
    display: inline-block;

    padding: 8px 14px;

    background: rgba(255,255,255,0.1);

    border: 1px solid rgba(255,255,255,0.2);

    border-radius: 30px;

    font-size: 11px;

    margin-bottom: 22px;
}

.hero h1 {
    font-family: "Playfair Display", serif;

    font-size: clamp(50px, 7vw, 82px);

    line-height: 1.02;

    margin-bottom: 22px;
}

.hero h1 span {
    display: block;

    color: #b9ead5;
}

.hero p {
    max-width: 600px;

    color: #d3e6e1;

    font-size: 17px;
}

.hero-buttons {
    display: flex;

    gap: 15px;

    margin-top: 32px;
}

.primary-button,
.secondary-button {
    padding: 14px 23px;

    border-radius: 30px;

    font-size: 12px;
    font-weight: 700;

    transition: 0.3s;
}

.primary-button {
    background: #c9eddb;
    color: #288d60;
}

.primary-button:hover {
    transform: translateY(-3px);

    box-shadow: 0 12px 30px rgba(0,0,0,0.18);
}

.secondary-button {
    border: 1px solid rgba(255,255,255,0.5);

    color: white;
}

.secondary-button:hover {
    background: white;
    color: #086c21;
}

.hero-stats {
    display: flex;

    gap: 45px;

    margin-top: 50px;
}

.hero-stats div {
    display: flex;
    flex-direction: column;
}

.hero-stats strong {
    font-size: 27px;
}

.hero-stats span {
    color: #bdd5cf;

    font-size: 10px;
}


/* =====================================================
   QUICK ACTIONS
===================================================== */

.quick-section {
    margin-top: -55px;

    position: relative;

    z-index: 10;
}

.quick-grid {
    display: grid;

    grid-template-columns: repeat(4, 1fr);

    gap: 15px;
}

.quick-card {
    background: white;

    border-radius: 16px;

    padding: 20px;

    display: flex;
    align-items: center;

    gap: 12px;

    box-shadow: 0 15px 40px rgba(15,70,65,0.12);

    transition: 0.3s;
}

.quick-card:hover {
    transform: translateY(-6px);
}

.quick-icon {
    width: 45px;
    height: 45px;

    flex-shrink: 0;

    border-radius: 12px;

    background: #e0f3ea;

    display: flex;
    align-items: center;
    justify-content: center;

    font-size: 20px;
}

.quick-card h3 {
    font-size: 13px;
}

.quick-card p {
    font-size: 9px;

    color: #7c8d89;
}

.quick-card > span {
    margin-left: auto;

    color: #147753;
}


/* =====================================================
   HEADINGS
===================================================== */

.section-heading {
    max-width: 700px;

    margin-bottom: 50px;
}

.section-heading.center {
    margin-left: auto;
    margin-right: auto;

    text-align: center;
}

.section-heading > span,
.section-label,
.appointment-info > span {
    color: #137842;

    font-size: 10px;

    letter-spacing: 3px;

    font-weight: 700;

    display: inline-block;

    margin-bottom: 14px;
}

.section-heading h2,
.about-text h2,
.collection-section h2,
.reports-section h2,
.faq-grid h2,
.contact-section h2 {
    font-family: "Playfair Display", serif;

    font-size: clamp(38px, 5vw, 57px);

    line-height: 1.1;

    font-weight: 600;

    margin-bottom: 18px;
}

.section-heading h2 strong,
.about-text h2 strong,
.collection-section h2 strong,
.reports-section h2 strong,
.faq-grid h2 strong,
.contact-section h2 strong {
    color: #13786e;
}

.section-heading p,
.about-text > p,
.collection-section > p,
.reports-section p,
.faq-grid > div > p,
.contact-section > div > p {
    color: #71827e;

    font-size: 14px;
}


/* =====================================================
   WHY
===================================================== */

.why-section {
    background: #f7faf8;
}

.why-grid {
    display: grid;

    grid-template-columns: repeat(4, 1fr);

    gap: 20px;
}

.why-card {
    background: white;

    padding: 30px;

    border: 1px solid #e4eeea;

    border-radius: 20px;

    transition: 0.3s;
}

.why-card:hover {
    transform: translateY(-6px);

    box-shadow: 0 20px 45px rgba(15,90,80,0.1);
}

.why-card .icon {
    width: 48px;
    height: 48px;

    border-radius: 14px;

    background: #def2e9;

    color: #13784c;

    display: flex;
    align-items: center;
    justify-content: center;

    margin-bottom: 20px;
}

.why-card h3 {
    font-family: "Playfair Display", serif;

    font-size: 21px;

    margin-bottom: 10px;
}

.why-card p {
    color: #71827e;

    font-size: 12px;
}


/* =====================================================
   ABOUT
===================================================== */

.about-section {
    background: white;
}

.about-grid {
    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 75px;

    align-items: center;
}

.about-image {
    min-height: 550px;

    position: relative;

    border-radius: 28px;

    background:
        linear-gradient(
            transparent 45%,
            rgba(3,60,55,0.75)
        ),
        url("https://images.unsplash.com/photo-1584982751601-97dcc096659c?auto=format&fit=crop&w=1000&q=90");

    background-size: cover;

    background-position: center;
}

.experience {
    position: absolute;

    bottom: 25px;
    left: 25px;

    background: white;

    padding: 22px;

    width: 185px;

    border-radius: 17px;

    box-shadow: 0 20px 40px rgba(0,0,0,0.15);
}

.experience small {
    color: #7b8c88;

    font-size: 10px;
}

.experience strong {
    display: block;

    font-family: "Playfair Display", serif;

    font-size: 42px;

    color: #137748;
}

.experience p {
    font-size: 10px;

    color: #71827e;
}

.about-text > p {
    margin-bottom: 16px;
}

.about-text ul {
    list-style: none;

    margin: 25px 0;
}

.about-text li {
    font-size: 12px;

    padding: 7px 0;

    color: #526762;
}

.text-button {
    color: #137765;

    font-size: 12px;

    font-weight: 700;
}


/* =====================================================
   SERVICES
===================================================== */

.services-section {
    background: #f4f9f7;
}

.services-grid {
    display: grid;

    grid-template-columns: repeat(4, 1fr);

    gap: 18px;
}

.service-card {
    background: white;

    min-height: 245px;

    padding: 27px;

    border-radius: 20px;

    border: 1px solid #e2ece8;

    transition: 0.3s;
}

.service-card:hover {
    transform: translateY(-6px);

    box-shadow: 0 20px 40px rgba(15,90,80,0.1);
}

.service-icon {
    font-size: 30px;

    margin-bottom: 18px;
}

.service-card h3 {
    font-family: "Playfair Display", serif;

    font-size: 21px;

    margin-bottom: 8px;
}

.service-card p {
    color: #71827e;

    font-size: 11px;

    margin-bottom: 18px;
}

.service-card a {
    color: #13776d;

    font-size: 11px;

    font-weight: 700;
}


/* =====================================================
   PACKAGES
===================================================== */

.packages-section {
    background: white;
}

.packages-grid {
    display: grid;

    grid-template-columns: repeat(3, 1fr);

    gap: 25px;
}

.package-card {
    background: #f7faf8;

    border: 1px solid #e2ebe7;

    border-radius: 22px;

    padding: 32px;
}

.package-card.popular {
    background: #0a5c56;

    color: white;

    transform: translateY(-8px);

    border: none;
}

.package-card small {
    color: #14776d;

    letter-spacing: 2px;

    font-weight: 700;

    font-size: 9px;
}

.package-card.popular small {
    color: #afe5d0;
}

.package-card label {
    float: right;

    background: #bcead5;

    color: #09695f;

    border-radius: 20px;

    padding: 5px 9px;

    font-size: 8px;

    font-weight: 700;
}

.package-card h3 {
    font-family: "Playfair Display", serif;

    font-size: 27px;

    margin: 17px 0 8px;
}

.package-card > p {
    color: #71827e;

    font-size: 11px;
}

.package-card.popular > p {
    color: #c9ddd8;
}

.package-card ul {
    list-style: none;

    margin: 23px 0;
}

.package-card li {
    padding: 6px 0;

    font-size: 11px;

    color: #566b66;
}

.package-card.popular li {
    color: #d3e5e1;
}

.package-card a {
    color: #14776d;

    font-size: 11px;

    font-weight: 700;
}

.package-card.popular a {
    color: #b5e9d4;
}


/* =====================================================
   HOME COLLECTION
===================================================== */

.collection-section {
    background: #f3f9f6;
}

.collection-grid {
    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 70px;

    align-items: center;
}

.collection-section > p {
    max-width: 500px;

    margin-bottom: 25px;
}

.collection-list {
    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 13px;

    margin: 25px 0;
}

.collection-list span {
    font-size: 11px;

    color: #526762;
}

.collection-image {
    min-height: 480px;

    border-radius: 27px;

    background:
        url("https://images.unsplash.com/photo-1603398938378-e54eab446dde?auto=format&fit=crop&w=1200&q=90");

    background-size: cover;

    background-position: center;
}


/* =====================================================
   PROCESS
===================================================== */

.process-section {
    background: white;
}

.process-grid {
    display: grid;

    grid-template-columns: repeat(4, 1fr);

    gap: 20px;
}

.process-card {
    text-align: center;

    padding: 20px;
}

.process-card > div {
    width: 62px;
    height: 62px;

    border-radius: 50%;

    background: #def2e9;

    color: #13776d;

    display: flex;
    align-items: center;
    justify-content: center;

    margin: 0 auto 20px;

    font-weight: 700;
}

.process-card h3 {
    font-family: "Playfair Display", serif;

    font-size: 20px;

    margin-bottom: 7px;
}

.process-card p {
    color: #71827e;

    font-size: 11px;
}


/* =====================================================
   REPORTS
===================================================== */

.reports-section {
    background: #eaf5f0;
}

.reports-grid {
    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 70px;

    align-items: center;
}

.report-box {
    background: white;

    padding: 32px;

    border-radius: 22px;

    box-shadow: 0 20px 45px rgba(15,80,70,0.1);
}

.report-box h3 {
    font-family: "Playfair Display", serif;

    font-size: 27px;
}

.report-box p {
    margin-bottom: 20px;
}

.report-box label {
    display: block;

    font-size: 10px;

    font-weight: 700;

    margin-bottom: 5px;
}

.report-box input {
    width: 100%;

    padding: 12px;

    border: 1px solid #dce8e4;

    border-radius: 9px;

    margin-bottom: 15px;

    outline: none;
}

.report-box button {
    width: 100%;

    padding: 13px;

    border: none;

    border-radius: 9px;

    background: #13776d;

    color: white;

    cursor: pointer;

    font-weight: 700;
}


/* =====================================================
   TESTIMONIALS
===================================================== */

.testimonials-section {
    background: #f7faf8;
}

.testimonial-grid {
    display: grid;

    grid-template-columns: repeat(3, 1fr);

    gap: 20px;
}

.testimonial {
    background: white;

    border: 1px solid #e4eeea;

    border-radius: 20px;

    padding: 28px;
}

.stars {
    color: #e3a42c;

    letter-spacing: 2px;

    margin-bottom: 15px;
}

.testimonial p {
    color: #5e716c;

    font-size: 12px;

    margin-bottom: 20px;
}

.testimonial strong {
    display: block;

    font-size: 12px;
}

.testimonial small {
    color: #879692;

    font-size: 9px;
}


/* =====================================================
   FAQ
===================================================== */

.faq-section {
    background: white;
}

.faq-grid {
    display: grid;

    grid-template-columns: 0.8fr 1.2fr;

    gap: 80px;
}

.faq-list {
    display: flex;

    flex-direction: column;

    gap: 10px;
}

.faq-list details {
    background: #f5f9f7;

    padding: 17px 20px;

    border-radius: 11px;
}

.faq-list summary {
    list-style: none;

    cursor: pointer;

    display: flex;

    justify-content: space-between;

    font-size: 12px;

    font-weight: 600;
}

.faq-list summary::-webkit-details-marker {
    display: none;
}

.faq-list summary span {
    color: #13776d;

    font-size: 18px;
}

.faq-list details p {
    color: #71827e;

    font-size: 11px;

    padding-top: 12px;
}


/* =====================================================
   APPOINTMENT
===================================================== */

.appointment-section {
    background: #07534e;

    color: white;
}

.appointment-grid {
    display: grid;

    grid-template-columns: 0.85fr 1.15fr;

    gap: 70px;

    align-items: center;
}

.appointment-info > span {
    color: #a9e4ca;
}

.appointment-info h2 {
    font-family: "Playfair Display", serif;

    font-size: clamp(38px, 5vw, 57px);

    line-height: 1.1;

    margin-bottom: 18px;
}

.appointment-info h2 strong {
    color: #b9ead5;
}

.appointment-info > p {
    color: #c6ddd8;

    font-size: 14px;
}

.contact-mini {
    margin-top: 30px;
}

.contact-mini p {
    margin: 12px 0;

    color: #cce1dc;

    font-size: 12px;
}

.appointment-form {
    background: white;

    color: #193c3a;

    padding: 32px;

    border-radius: 23px;
}

.appointment-form h3 {
    font-family: "Playfair Display", serif;

    font-size: 27px;

    margin-bottom: 22px;
}

.form-row {
    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 15px;

    margin-bottom: 15px;
}

.appointment-form label {
    display: block;

    font-size: 10px;

    font-weight: 700;

    margin-bottom: 5px;
}

.appointment-form input,
.appointment-form select,
.appointment-form textarea {
    width: 100%;

    padding: 12px;

    border: 1px solid #dce8e4;

    border-radius: 8px;

    background: #fafcfb;

    outline: none;

    font-size: 11px;
}

.appointment-form textarea {
    resize: vertical;

    margin-bottom: 15px;
}

.appointment-form button {
    width: 100%;

    border: none;

    padding: 14px;

    background: #13776d;

    color: white;

    border-radius: 8px;

    font-size: 11px;

    font-weight: 700;

    cursor: pointer;
}


/* =====================================================
   CONTACT
===================================================== */

.contact-section {
    background: white;
}

.contact-grid {
    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 70px;

    align-items: center;
}

.contact-details {
    display: grid;

    gap: 18px;

    margin-top: 25px;
}

.contact-details > div {
    display: flex;

    gap: 12px;
}

.contact-details > div > span {
    width: 38px;
    height: 38px;

    flex-shrink: 0;

    border-radius: 10px;

    background: #def2e9;

    display: flex;
    align-items: center;
    justify-content: center;
}

.contact-details strong {
    font-size: 11px;
}

.contact-details p {
    color: #71827e;

    font-size: 11px;
}

.map-box {
    min-height: 430px;

    border-radius: 25px;

    display: flex;

    align-items: center;
    justify-content: center;

    text-align: center;

    color: white;

    background:
        linear-gradient(
            rgba(4,76,69,0.85),
            rgba(4,76,69,0.85)
        ),
        url("https://images.unsplash.com/photo-1449824913935-59a10b8d2000?auto=format&fit=crop&w=1200&q=90");

    background-size: cover;

    background-position: center;
}

.map-pin {
    font-size: 40px;
}

.map-box h3 {
    font-family: "Playfair Display", serif;

    font-size: 28px;
}

.map-box p {
    color: #c8ded8;

    font-size: 12px;

    margin: 7px 0 20px;
}

.map-box a {
    display: inline-block;

    padding: 11px 18px;

    background: white;

    color: #13776d;

    border-radius: 25px;

    font-size: 10px;

    font-weight: 700;
}


/* =====================================================
   FINAL CTA
===================================================== */

.final-cta {
    padding: 80px 0;

    text-align: center;

    color: white;

    background: linear-gradient(
        120deg,
        #0a6860,
        #0d8174
    );
}

.final-cta span {
    color: #bce9d5;

    font-size: 9px;

    letter-spacing: 3px;

    font-weight: 700;
}

.final-cta h2 {
    font-family: "Playfair Display", serif;

    font-size: clamp(40px, 6vw, 60px);

    margin: 8px 0;
}

.final-cta p {
    color: #d1e5df;

    font-size: 13px;

    margin-bottom: 25px;
}

.final-cta a {
    display: inline-block;

    padding: 13px 22px;

    border-radius: 30px;

    margin: 4px;

    font-size: 11px;

    font-weight: 700;

    background: white;

    color: #0b6b62;
}

.final-cta a:last-child {
    background: transparent;

    color: white;

    border: 1px solid rgba(255,255,255,0.5);
}


/* =====================================================
   FOOTER
===================================================== */

footer {
    background: #063d3a;

    color: white;

    padding-top: 55px;
}

.footer-grid {
    display: grid;

    grid-template-columns: 1.5fr 1fr 1fr 1fr;

    gap: 45px;

    padding-bottom: 45px;
}

.footer-description {
    color: #b3ccc6;

    font-size: 11px;

    max-width: 260px;

    margin-top: 18px;
}

footer h4 {
    font-size: 12px;

    margin-bottom: 17px;
}

footer > .container a,
footer > .container p {
    color: #b3ccc6;

    font-size: 10px;

    margin-bottom: 8px;
}

footer a:hover {
    color: white;
}

.footer-bottom {
    border-top: 1px solid rgba(255,255,255,0.1);

    padding: 18px 0;
}

.footer-bottom .container {
    display: flex;

    justify-content: space-between;

    align-items: center;
}

.footer-bottom p,
.footer-bottom a {
    font-size: 9px !important;

    color: #8fa9a3 !important;
}

.footer-bottom a {
    margin-left: 15px;
}


/* =====================================================
   WHATSAPP
===================================================== */

.whatsapp {
    position: fixed;

    right: 20px;
    bottom: 20px;

    width: 53px;
    height: 53px;

    border-radius: 50%;

    display: flex;
    align-items: center;
    justify-content: center;

    background: #25d366;

    color: white;

    font-size: 23px;

    z-index: 999;

    box-shadow: 0 10px 30px rgba(0,0,0,0.2);

    transition: 0.3s;
}

.whatsapp:hover {
    transform: scale(1.08);
}


/* =====================================================
   LAPTOP
===================================================== */

@media (max-width: 1100px) {

    .nav-links {
        display: none;
    }

    .quick-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .why-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .services-grid {
        grid-template-columns: repeat(2, 1fr);
    }

}


/* =====================================================
   TABLET
===================================================== */

@media (max-width: 850px) {

    .about-grid,
    .collection-grid,
    .reports-grid,
    .appointment-grid,
    .contact-grid,
    .faq-grid {
        grid-template-columns: 1fr;

        gap: 45px;
    }

    .packages-grid {
        grid-template-columns: 1fr;
    }

    .package-card.popular {
        transform: none;
    }

    .process-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .testimonial-grid {
        grid-template-columns: 1fr;
    }

    .footer-grid {
        grid-template-columns: 1fr 1fr;
    }

}


/* =====================================================
   MOBILE
===================================================== */

@media (max-width: 600px) {

    /* GLOBAL */

    .container {
        width: 92%;
    }

    .section {
        padding: 70px 0;
    }


    /* TOP BAR */

    .top-bar {
        display: none;
    }


    /* NAVBAR */

    .navbar {
        top: 0;

        padding: 13px 0;
    }

    .logo-icon {
        width: 37px;
        height: 37px;

        font-size: 23px;
    }

    .logo strong {
        font-size: 19px;
    }

    .logo small {
        font-size: 7px;
    }

    .nav-button {
        padding: 9px 12px;

        font-size: 9px;
    }


    /* HERO */

    .hero {
        min-height: 700px;
    }

    .hero-overlay {
        background:
            linear-gradient(
                90deg,
                rgba(2,48,45,0.97),
                rgba(2,57,53,0.68)
            ),
            url("https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?auto=format&fit=crop&w=1200&q=85");

        background-position: 65% center;
    }

    .hero-text {
        padding-top: 60px;
    }

    .hero h1 {
        font-size: 48px;
    }

    .hero p {
        font-size: 14px;
    }

    .hero-buttons {
        flex-direction: column;
    }

    .primary-button,
    .secondary-button {
        text-align: center;
    }

    .hero-stats {
        gap: 20px;

        flex-wrap: wrap;

        margin-top: 38px;
    }

    .hero-stats strong {
        font-size: 22px;
    }


    /* QUICK ACTIONS */

    .quick-section {
        margin-top: -30px;
    }

    .quick-grid {
        grid-template-columns: 1fr;
    }


    /* WHY */

    .why-grid {
        grid-template-columns: 1fr;
    }


    /* ABOUT */

    .about-image {
        min-height: 400px;
    }

    .experience {
        bottom: 18px;
        left: 18px;
    }


    /* SERVICES */

    .services-grid {
        grid-template-columns: 1fr;
    }


    /* COLLECTION */

    .collection-list {
        grid-template-columns: 1fr;
    }

    .collection-image {
        min-height: 350px;
    }


    /* PROCESS */

    .process-grid {
        grid-template-columns: 1fr;
    }


    /* REPORT */

    .report-box {
        padding: 25px;
    }


    /* APPOINTMENT */

    .form-row {
        grid-template-columns: 1fr;
    }

    .appointment-form {
        padding: 25px;
    }


    /* CONTACT */

    .map-box {
        min-height: 330px;
    }


    /* FOOTER */

    .footer-grid {
        grid-template-columns: 1fr;

        gap: 30px;
    }

    .footer-bottom .container {
        flex-direction: column;

        text-align: center;

        gap: 10px;
    }


    /* WHATSAPP */

    .whatsapp {
        width: 48px;
        height: 48px;

        right: 15px;
        bottom: 15px;
    }

}


/* =====================================================
   VERY SMALL PHONES
===================================================== */

@media (max-width: 380px) {

    .hero h1 {
        font-size: 40px;
    }

    .nav-button {
        padding: 8px 9px;

        font-size: 8px;
    }

    .logo strong {
        font-size: 17px;
    }

}